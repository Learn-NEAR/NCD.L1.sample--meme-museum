{
  "name": "hello_near",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "type": "module",
  "scripts": {
    "build": "npm run build:meme && npm run build:museum",
    "build:meme": "near-sdk-js build src/meme/contract.ts build/meme.wasm",
    "build:museum": "near-sdk-js build src/museum/contract.ts build/museum.wasm",
    "deploy": "npm run build && rm -rf neardev && near dev-deploy --wasmFile build/hello_near.wasm && export $(cat ./neardev/dev-account.env) && near call $CONTRACT_NAME init --accountId $CONTRACT_NAME --deposit 1 && echo $CONTRACT_NAME",
    "test": "echo use integration-tests"
  },
  "dependencies": {
    "near-cli": "^3.4.0",
    "near-sdk-js": "0.4.0-5"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
